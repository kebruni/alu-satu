# ⚙️ Конфигурация для домена alusatu.kebruni.me

## Что писать в `.env` файл

### Ваша конфигурация:

```env
# ========== BACKEND (Node.js) ==========

# Режим - оставить как есть
NODE_ENV=production

# Порт бэкенда - оставить как есть
PORT=4000

# БД - оставить как есть (это имя Docker сервиса)
MONGO_URI=mongodb://admin:password123@mongodb:27017/alu-satu?authSource=admin

# 🔐 Секретный ключ для токенов - ИЗМЕНИТЕ на сложный!
JWT_SECRET=mK9x#pL@2kq$X9vM%nP5Rw8YxZa1Bc3De5Fg7Hi9Jk

# ✅ Ваш домен - откуда разрешены запросы
CORS_ORIGIN=https://alusatu.kebruni.me

# ========== FRONTEND (React Vite) ==========

# API endpoint для фронтенда
VITE_API_URL=https://alusatu.kebruni.me/api
```

---

## Пошагово - что куда пишется

### 1️⃣ NODE_ENV
```
Что: Окружение разработки
Для вас: production
Менять: НЕТ
```

### 2️⃣ PORT
```
Что: На каком порту запускается бэкенд внутри контейнера
Для вас: 4000
Менять: НЕТ
```

### 3️⃣ MONGO_URI
```
Что: Адрес подключения к БД
Для вас: mongodb://admin:password123@mongodb:27017/alu-satu?authSource=admin
Менять: НЕТ (это имя Docker сервиса "mongodb")
```

### 4️⃣ JWT_SECRET ⚠️ ВАЖНО!
```
Что: Секретный ключ для шифрования токенов авторизации
Для вас: Любой сложный пароль из букв, цифр, спецсимволов
Пример: mK9x#pL@2kq$X9vM%nP5Rw8YxZa1Bc3De5Fg7Hi9Jk
Менять: ДА! Сгенерируйте свой
```

**Как сгенерировать безопасный ключ:**
```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
[Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Random -Count 32 | % {[char]$_}) -join ""))
```

### 5️⃣ CORS_ORIGIN ✅ ДЛЯ ВАШЕГО ДОМЕНА
```
Что: С каких доменов фронтенд может обращаться к бэкенду
Для вас: https://alusatu.kebruni.me
Менять: ДА! Вставьте ваш домен
```

### 6️⃣ VITE_API_URL ✅ ДЛЯ ВАШЕГО ДОМЕНА
```
Что: Адрес API для фронтенд приложения
Для вас: https://alusatu.kebruni.me/api
Менять: ДА! Вставьте ваш домен + /api
```

---

## 📋 Готовая инструкция

### На вашем Debian сервере:

```bash
# 1. Перейдите в папку проекта
cd /opt/alu-sату

# 2. Создайте файл .env
nano .env

# 3. Вставьте это:
NODE_ENV=production
PORT=4000
MONGO_URI=mongodb://admin:password123@mongodb:27017/alu-satu?authSource=admin
JWT_SECRET=mK9x#pL@2kq$X9vM%nP5Rw8YxZa1Bc3De5Fg7Hi9Jk
CORS_ORIGIN=https://alusatu.kebruni.me
VITE_API_URL=https://alusatu.kebruni.me/api

# 4. Нажмите Ctrl+X, потом Y, потом Enter (если используете nano)

# 5. Проверьте что создался
cat .env

# 6. Запустите проект
docker-compose up -d
```

---

## 🔗 Как будет работать?

```
┌─────────────────────────────────────────┐
│  Браузер пользователя                   │
│  https://alusatu.kebruni.me             │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  NGINX (Reverse Proxy)                  │
│  порт 80 и 443 (HTTPS)                  │
│  на сервере Debian 12                   │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    ▼                 ▼
┌─────────────┐   ┌─────────────┐
│ Frontend    │   │ Backend API │
│ :3000       │   │ :4000       │
│ React+Vite  │   │ Node.js     │
└─────────────┘   └──────┬──────┘
                         │
                         ▼
                  ┌─────────────┐
                  │ MongoDB     │
                  │ :27017      │
                  └─────────────┘
```

---

## ⚡ Таблица всех переменных

| Переменная | Что это | Значение для вас | Менять? |
|-----------|--------|-----------------|---------|
| `NODE_ENV` | Режим | `production` | ❌ НЕТ |
| `PORT` | Порт бэкенда | `4000` | ❌ НЕТ |
| `MONGO_URI` | БД подключение | `mongodb://admin:password123@mongodb:27017/alu-satu?authSource=admin` | ❌ НЕТ |
| `JWT_SECRET` | 🔐 Секретный ключ | ВАШ СЛОЖНЫЙ ПАРОЛЬ | ✅ ДА! |
| `CORS_ORIGIN` | ✅ Домен фронта | `https://alusatu.kebruni.me` | ✅ ДА! |
| `VITE_API_URL` | ✅ Адрес API | `https://alusatu.kebruni.me/api` | ✅ ДА! |

---

## Проверка на ошибки

```bash
# Проверить что .env создан
cat /opt/alu-satu/.env

# Проверить логи фронтенда
docker logs alu-satu-frontend

# Проверить логи бэкенда
docker logs alu-satu-backend

# Проверить что контейнеры запущены
docker ps
```

## Контакт для помощи
Если что-то не работает, проверьте:
1. ✅ Домен corrnet в `CORS_ORIGIN`
2. ✅ `/api` в конце `VITE_API_URL`
3. ✅ `JWT_SECRET` - сложный пароль
4. ✅ HTTPS протокол (не http)
